// console.log("Hi There!");
class Timer {
    constructor(message){
        console.log(message);
    }
}
// when we call a new instance the constructor is auotmatically run for us.
new Timer('hi there!');